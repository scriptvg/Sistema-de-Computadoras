import { SuccessAlert, ErrorAlert, WarningAlert, ConfirmAlert } from './types.js';

export const Alerts = {
    success: SuccessAlert,
    error: ErrorAlert,
    warning: WarningAlert,
    confirm: ConfirmAlert
};